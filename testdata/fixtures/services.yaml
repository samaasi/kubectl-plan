apiVersion: v1
kind: Service
metadata:
  name: payment-api-svc
  namespace: production
  labels:
    app: payment-api
spec:
  selector:
    app: payment-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: checkout-svc
  namespace: production
  labels:
    app: checkout-service
spec:
  selector:
    app: checkout-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: billing-svc
  namespace: billing
  labels:
    app: billing-service
spec:
  selector:
    app: billing-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
# Dangling service — no matching pods (garbage-collect fixture)
apiVersion: v1
kind: Service
metadata:
  name: old-auth-svc
  namespace: production
  labels:
    app: old-auth
  creationTimestamp: "2025-01-01T00:00:00Z"
spec:
  selector:
    app: old-auth
  ports:
    - name: http
      port: 8080
      targetPort: 8080
